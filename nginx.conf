# server {
#     listen 80;
#     server_name localhost;

#     # Point Nginx to Laravel's public folder
#     root /var/www/html/public;
#     index index.php index.html;

#     # Handle requests to the Laravel backend
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     # Pass PHP scripts to PHP-FPM (running in a separate container)
#     location ~ \.php$ {
#         fastcgi_pass app:9000;  # Connect to PHP-FPM (defined in your Docker Compose)
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         include fastcgi_params;
#     }

#     # Serve static files
#     location ~* \.(jpg|jpeg|png|gif|css|js|ico|svg)$ {
#         try_files $uri =404;
#         expires max;
#         log_not_found off;
#     }

#     # Handle Vite (React) if necessary for frontend
#     location /frontend {
#         proxy_pass http://vite:3000;  # Proxy requests to Vite development server
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
